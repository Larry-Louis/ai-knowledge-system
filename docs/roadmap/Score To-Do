# Score To-Do

日期：2026-07-01

## 一、整体情况（基于 Score Spec 对当前实现的评估）

评估对象：`rag-api/domain/memory/rule_evaluator.py`
对照文档：`docs/rule-score/Score Spec.md`

### 1) 总体结论

- 当前实现方向正确，已从“技术模块划分”逐步转向“记忆价值三维划分”。
- 已具备可运行基线，但与 Spec 的“高一致性三维正交”仍有中等偏差。

粗略达成度（用于路线优先级判断）：
- 第一维 Semantic Relevance：80%~85%
- 第二维 Information Density：65%~75%
- 第三维 Personal Commitment：70%~80%
- 三维融合与去重叠：55%~65%

### 2) 现状亮点

- 第三维已从旧结构启发式切到 Personal Commitment 信号（Identity/Preference/Plan/Long-term/Opinion）。
- 第二维已使用信息密度接口并输出 evidence/meta，具备可解释性。
- 第一维以 embedding 语义为主并引入 non-memory 抑制，基本符合“语义主导”方向。

### 3) 关键偏差

- 第三维尚未真正“由第二维升级而来”：
  - 当前第三维主要是承诺词命中，缺少“承诺对象绑定”（承诺与实体主题的耦合强度）。
- 第二维存在职责混入：
  - `DISSOLVE_WORDS` 的撤回/玩笑语义属于准入控制，不应成为信息密度主判据。
- 三维有重复加分风险：
  - `calculate_rule_score = pss + dps + mdp` 直接相加后截断，可能掩盖维度边界与解释性。

---

## 二、修改方向（本阶段执行清单）

> 目标：将实现收敛到 Score Spec 的三维定义，并提升可解释性与可回归性。

### P0（优先立即实施）

1. 第三维引入“承诺信号 × 信息对象绑定”
- 方向：在 `probe_structure_score` 中使用第二维产出的实体/evidence（或等价对象信息）。
- 核心：
  - 有承诺词 + 有可锚定对象（如 Rust/ARA::COM/项目名）=> 显著加分
  - 有承诺词但无对象 => 降权
  - 纯客观事实且无第一人称 => 保持低分
- 验收：
  - `Rust是一门语言` < `我计划学Rust` < `我以后准备长期在项目中使用Rust`

### P1（紧随其后）

2. 清理第二维职责边界
- 方向：将 `DISSOLVE_WORDS` 从信息密度函数中迁移到准入层/前置过滤层。
- 目标：保证第二维只回答“信息是否具体、可索引”。
- 验收：
  - 第二维评分与“撤回意图”解耦，避免误伤信息密度。

3. 增加第三维语境鲁棒性规则
- 方向：补充主语一致性、引述/转述过滤、假设语气弱化。
- 目标：避免“他说他计划…”、“如果我以后…”等句子被高估。
- 验收：
  - 转述/假设样本相对第一人称直陈样本得分更低。

4. 三维融合去重叠
- 方向：在总分聚合前加入维度抑制/归一化策略，减少重复奖励。
- 目标：提升总分可解释性，避免“同一语义多次加分”。
- 验收：
  - 日志可说明每维独立贡献及抑制来源。

### P1（评估与回归）

5. 建立评分回归样本集（最小可用版）
- 规模：30~50 条起步（后续扩到 100~300）。
- 分层：高/中/低 commitment + 高/低 information + 高/低 semantic。
- 断言：
  - 事实类 < 个人长期承诺类
  - 泛化描述 < 具体实体描述
  - 非记忆问候/指令类保持低分

---

## 三、落地顺序建议

1. 先改第三维对象绑定（P0）
2. 再拆第二维职责（P1）
3. 再做三维聚合去重（P1）
4. 最后固化回归集与阈值（P1）

---

## 四、阶段产出定义

完成本 To-Do 后，评分系统应满足：
- 三维各司其职且边界清晰；
- 第三维真正反映“用户-主题持续关系”；
- 总分日志可解释、可回归、可调参。
