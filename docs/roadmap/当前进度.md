# AI Knowledge System — 当前进度

日期：2026-05-31

## 已完成

### Phase 1 — Memory Admission System（全部完成）
- Stage 0: Turn 聚合 + 异步管道 + SLM 评分 + Qdrant 写入 ✅
- Stage 1: SQLite 持久化队列 + 重试 + 崩溃恢复 ✅
- Stage 2: SLM 输出标准化 + 置信度阈值 + type mapping ✅
- Stage 3: Turn 上下文完整（user+assistant）✅
- Stage 4: MU 提取器（多摘要+规则后备）✅
- Stage 5: Normalizer（主语统一+术语归一）+ Dedup（余弦去重>0.9跳过）✅
- Stage 6: Conflict Resolver（极性矛盾检测+LLM仲裁）✅

### 1. 架构重写
- 删除 `VectorStoreIndex([])` 和 llama_index 依赖
- 实现三层记忆系统：短期（最近20轮）+ 长期（Qdrant）+ 全局摘要
- LLM 适配层：抽象基类 + Ollama / DeepSeek / OpenAI 三套实现
- OpenAI 兼容 API：`POST /v1/chat/completions`

### 2. LLM 切换为 DeepSeek
- 默认 LLM 从 Ollama(qwen3.5:9b) 切换为 DeepSeek(deepseek-v4-flash/pro)
- API Key 通过 `.env` 文件配置，docker-compose 自动读取
- 支持 Open WebUI 实时切换模型（flash ↔ pro）

### 3. 思维链展示
- 透传 DeepSeek `reasoning_content` 到 API 响应
- Open WebUI 原生灰色可折叠思维链展示

### 4. 分层记忆系统
- 新增 `layer` 字段区分记忆层
- 预设三层：`general`（默认）、`story`（故事创作）、`docreader`（文档分析）
- `core` 层始终生效，不随切换改变
- API: `GET/POST /role` 切换当前层
- 模型名后缀 `:story` 或直接写 `story` 自动切换

### 5. 文档分析服务 (doc-reader)
- 独立服务 `doc-reader`（端口 19000），与 rag-api 完全隔离
- 上传 .txt / .pdf，自动按章节分块存入 Qdrant `documents` 集合
- 对话式查询：提问后自动搜索全文档相关内容
- Web 前端界面挂载/卸载文档

### 6. 文档挂载集成
- rag-api 对话时自动搜索已挂载文档的关联内容
- `POST /documents/active` 管理挂载列表
- 相似度阈值 0.65，防止无关内容混入

### 7. 基础设施
- Docker Ollama 运行 nomic-embed-text（274MB）
- 移除 host.docker.internal 依赖，全部走 Docker 内部网络
- Qdrant 新增 `documents` 集合 + timestamp 索引

### 8. 调试辅助
- `GET /v1/last-prompt` 查看最近一次完整 prompt
- 支持 `GET /role` 查看当前层和可用层

## 2026-06-07 架构重构记录
- 完成了项目架构的深度拆解与重构：
  - 抽离 Prompt 构造器：创建了 prompts/prompt_factory.py，实现了 Prompt 的独立配置与管理。
  - 抽离决策器：创建了 domain/memory/decision_maker.py，封装了核心的存储策略和分类逻辑。
  - 优化数据处理层：创建了 domain/memory/text_utils.py，将原本混杂在 pipeline 中的数据处理 helper 函数进行了模块化归位。
  - 实现中间件隔离：通过重构 memory_pipeline_service.py，实现了业务流程编排与 LLM 调用、数据处理逻辑的彻底解耦。
  - 架构验收：当前系统已实现业务与 LLM 隔离，各模块职责清晰，具备良好的扩展性。

当前架构已从“面条式”成功转化为“积木式”，后续开发可直接基于模块化的接口进行扩展。

## 2026-06-09 架构重构与 NLP 任务规划
- 修复 Docker 网络配置：将 OLLAMA_BASE_URL 统一为容器内服务名 'ollama'
- 实现 LLM 智能动态路由：根据 model ID 自动在 Ollama/DeepSeek/OpenAI 间切换
- 配置化路由解析：将层级与模型列表移入 Config 统一管理
- 记忆管道健壮性修复：修复 f-string 语法错误，增强异常捕获
- 自动索引维护：在 Qdrant 初始化时自动创建 timestamp 索引
- 切换 NLP 分支：开启 codex/nlp-tasks 任务流

## 待办

### P1 — 记忆可视化界面
- 可视化查看/编辑各层记忆
- 按层筛选、按时间排序、搜索内容
- 删除单条/批量删除记忆
- 见项目记忆：memory-visualization-ui

### P2 — 优化
- 记忆量大时 `get_recent_global_memories` 的时间排序性能
- 大文档（>50MB）的分章和向量化效率
- 添加 streaming 支持

### P3 — 长期
- 升级 embedding 模型
- 减容器镜像体积
